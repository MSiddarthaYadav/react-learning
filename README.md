# 📘 React Learning Repository

This repository contains my hands-on practice and learning files for React concepts, covering fundamentals to advanced topics. Each folder demonstrates a specific concept with simple examples for better understanding.

---

## 🚀 Topics Covered

### 🔹 Basics

* JSX
* Components (Functional & Class)
* Props
* State
* Lifecycle Methods

### 🔹 Rendering & Events

* Conditional Rendering
* Lists & Keys
* Event Handling
* Embedding Expressions

### 🔹 Forms & DOM

* Forms & Controlled Components
* Form Validation
* Refs
* Forward Refs
* Lifting State Up

### 🔹 React Hooks

* useState
* useEffect
* useContext
* useRef
* useReducer
* useMemo
* useCallback
* Custom Hooks

### 🔹 Routing & Navigation

* React Router
* useNavigate

### 🔹 Advanced Concepts

* Props Drilling
* Error Handling

---

## 📂 Project Structure

Each folder represents a specific concept:

* `react-app` → Main React setup
* `app JSX example` → JSX basics
* `app Components` → Components
* `app PropState` → Props & State
* `app condtionalrendring` → Conditional rendering
* `app lists&keys` → Lists & keys
* `app eventhandling` → Events
* `app Mouseevent` → Mouse events
* `app forms&components` → Forms
* `app formvalidation` → Validation
* `app ref` → useRef
* `app forwardref` → Forward refs
* `app lifecyle` → Lifecycle
* `app liftingstateup` → Lifting state
* `app propsdrilling` → Props drilling
* `app errorhandling` → Error handling
* `app embeddingexpressions` → JSX expressions
* `app useState` → useState
* `app useEffect` → useEffect
* `app useContext` → useContext
* `app useReducer` → useReducer
* `app useMemo` → useMemo
* `app useCallback` → useCallback
* `app useNavigate` → Navigation
* `app useRef` → useRef
* `app useWindowWidth(Customhooks)` → Custom hook

---

## ▶️ How to Run

```bash
npm install
npm start
```

---

## 🎯 Purpose

This repository is created to:

* Practice React concepts
* Build strong frontend fundamentals
* Prepare for real-world projects and interviews

---

## 📌 Author

**M Siddartha Yadav**
